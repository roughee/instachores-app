# Schedule tab mockups

Static mockups for issue #64, drawn against the app's own tokens, type ramp and
components (`AppShell`, `TaskButton`, `TaskGroup`, `Sheet`). Editable canvas:
https://claude.ai/code/artifact/3e6daa1f-a290-48ca-87a2-ea1c81a8deef

| File | Screen |
| --- | --- |
| `schedule-tab-light.png`, `schedule-tab-dark.png` | The `#/schedule` tab: Today, Tomorrow, This week, then Recently done |
| `next-time-sheet-light.png` | The Next time? sheet after logging Clean bathroom, 7 days preselected |
| `category-scheduled-light.png` | Floors with a due task, last-done sub-lines and the Scheduled fold |

Regenerate by re-seeding the canvas from its artboards; these PNGs are exports of
those artboards at 390 x 844.
